<Window x:Class="NexusShell.App.Views.ThemeManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Theme Manager" Height="360" Width="430"
        Background="#1E1E1E" Foreground="White"
        WindowStartupLocation="CenterOwner"
        AutomationProperties.Name="Theme Manager"
        AutomationProperties.HelpText="Choose a color theme for the terminal. The selected theme is applied to the terminal immediately.">
    <Window.Resources>
        <Style x:Key="DarkBtn" TargetType="Button">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E3E"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#007ACC"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Select a Theme:"
                   Foreground="White" FontSize="14" FontWeight="SemiBold"
                   Margin="0,0,0,10"
                   AutomationProperties.HeadingLevel="Level1"/>

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Themes}"
                 SelectedItem="{Binding SelectedTheme}"
                 DisplayMemberPath="Name"
                 Background="#252526" Foreground="White" BorderBrush="#444444"
                 TabIndex="1"
                 AutomationProperties.Name="Available Themes"
                 AutomationProperties.HelpText="Use arrow keys to browse themes. Theme is applied to the terminal immediately when you select it.">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="AutomationProperties.Name" Value="{Binding Name}"/>
                    <Setter Property="AutomationProperties.HelpText">
                        <Setter.Value>
                            <MultiBinding StringFormat="Background color: {0}, Text color: {1}. Select to apply.">
                                <Binding Path="Background"/>
                                <Binding Path="Foreground"/>
                            </MultiBinding>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <TextBlock Grid.Row="2"
                   Text="Theme is applied immediately when selected."
                   FontSize="11" Foreground="#888888"
                   Margin="0,10,0,10"/>

        <Button Grid.Row="3" Content="_Close"
                IsCancel="True" Style="{StaticResource DarkBtn}"
                Height="32" HorizontalAlignment="Right" Width="90" TabIndex="2"
                AutomationProperties.Name="Close Theme Manager"
                AutomationProperties.HelpText="Close this window. Your selected theme has already been applied to the terminal."/>
    </Grid>
</Window>
