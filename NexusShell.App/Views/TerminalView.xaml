<UserControl x:Class="NexusShell.App.Views.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- Output is rendered by an accessible HTML command-block log inside WebView2
             (v4.0.0 model): a hidden xterm.js resolves the raw ConPTY stream to clean
             text, which is shown as <pre role="log" aria-live> blocks that JAWS/NVDA
             read. Input stays in the WPF command box (MainWindow). -->
        <wv2:WebView2 x:Name="WebTerminal"
                      AutomationProperties.Name="Terminal Output"
                      AutomationProperties.HelpText="Displays the output from the shell."
                      DefaultBackgroundColor="#1E1E1E" />
    </Grid>
</UserControl>
